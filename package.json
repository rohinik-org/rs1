{
  "name": "rohinik",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r --sort run build",
    "bundle": "node scripts/build-bundle.mjs",
    "test": "pnpm -r --sort run test",
    "typecheck": "pnpm -r --sort run typecheck"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rohinik-org/rs1.git"
  },
  "homepage": "https://github.com/rohinik-org/rs1#readme",
  "bugs": {
    "url": "https://github.com/rohinik-org/rs1/issues"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
