import type { ExecutionResult, MemoryCandidate } from '@rohinik-org/compiler'

export class ProceduralLearner {
  // ponytail: stub — real pattern detection requires corpus scan (Stage 6C)
  learn(_result: ExecutionResult): MemoryCandidate[] {
    return []
  }
}
