import type { ExecutionResult, MemoryCandidate } from '@rohinik-org/compiler'

export class SemanticExtractor {
  // ponytail: stub — real extraction requires concept model (Stage 6C)
  extract(_result: ExecutionResult): MemoryCandidate[] {
    return []
  }
}
