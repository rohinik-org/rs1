version: "1.0"
runtime:
  routing:
    mode: balanced
    explain: true
  logLevel: info
server:
  port: 8080
  host: 127.0.0.1
providers:
  openai:
    apiKey: ${OPENAI_API_KEY}
extensions:
  paths: []
